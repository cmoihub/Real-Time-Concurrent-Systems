Accompanied by the diagrams are the source files of the program which were used to test what the program does and further understand how to make the diagrams

The processItem method in the WorkQueue class is externally synchronized by the run method of its inner class (WorkerThread)

The clients generate integers to be enqueued by the DisplayQueue
The DisplayQueue puts these integers into a queue if it's a given client's turn and it prints what integer was put into the queue

Diagrams
-UML class diagram
-UML coIlaboration diagram
-UCM